---
type: Reference
---

# Products

Config for the product-aware skills: `bd-radar` and `product-pulse`.
One `##` block per product. Public repos only — this worker repo is public.

## Verdikta
- **repos:** `verdikta/verdikta-applications` (public), `verdikta/verdikta-arbiter` (public), `verdikta/verdikta-dispatcher` (public), `verdikta/verdikta-docs` (public), `verdikta/verdikta-roadmap` (public)
- **handles:** `@verdikta19633`
- **terms:** `Verdikta`, `AI-judged bounties`, `bounties.verdikta.org`, `AI arbiter Base`, `AI bounty escrow`
- **surface:** Decentralized AI-judged bounty escrow on Base — creators post ETH bounties with public rubrics, agents/hunters submit work via an open bot API, two independent AI models score against the rubric, and the escrow contract (`0x2Ae271f5E86bee449a36B943414b7C1a7b39772D`) pays winners trustlessly. Exposes: bounty API (`bounties.verdikta.org/api` + agents.txt), arbiter node stack, dispatcher contracts, reference bounty program (`example-bounty-program/` in verdikta-applications).
